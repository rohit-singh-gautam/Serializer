serializer version 1;
include ../catalog/inventory;
include ../audit/event;
include ../common/money;

namespace demo {
  class snapshot stable_ids {
    public array inventory stock (1);
    public array event events (2);
    public map(currency) money totals (3);
  }
}
