serializer version 1;
include sales/order;
include analytics/snapshot;
include common/identity;

class example_model stable_ids {
  public uint32 revision (1) { 1 };
  public string title (2);
  public array demo::order orders (3);
  public demo::snapshot snapshot (4);
  public map(uint64) demo::customer customers (5);
}
