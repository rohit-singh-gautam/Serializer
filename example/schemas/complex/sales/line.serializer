serializer version 1;
include ../catalog/product;
include ../common/money;

namespace demo {
  class order_line stable_ids {
    public product item (1);
    public uint32 quantity (2);
    public money subtotal (3);
  }
}
